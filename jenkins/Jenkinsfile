pipeline {
    agent any
    environment {
        JAVA_HOME = '/usr/lib/jvm/java-11'
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn clean install' // Or any other Maven command
            }
        }
    }
}

